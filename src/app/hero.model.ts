export class Hero {
  constructor(public name: string, public specialty: string, public race:string, public description: string, public blurb: string, public id: number, public featured: boolean = false) { }
}
